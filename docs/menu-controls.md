# User menu controls

The user menu is configurable from the bot admin menu editor. Each product button can be enabled or disabled independently. The subscription button shows current status and supports cancelling auto-renewal.
